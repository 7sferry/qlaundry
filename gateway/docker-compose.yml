# Made by [MR Ferry™] on Juli 2026

services:
  gateway:
    image: nginx:alpine
    container_name: qlaundry-gateway
    ports:
      - "8100:80"
      - "8443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro
#    restart: unless-stopped
